import styled from 'styled-components';

export const InputContainer = styled.div`
  position: relative;
  width: 22rem;

  svg {
    position: absolute;
    right: 1rem;
    top: 50%;
    transform: translateY(-50%);
  }
`;
