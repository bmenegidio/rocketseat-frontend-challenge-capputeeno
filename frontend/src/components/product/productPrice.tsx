import styled from 'styled-components';

export const ProductPrice = styled.span`
  color: ${(props) => props.theme.colors.shapeDarkTooltip};
  font-size: 1.25rem;
  font-weight: 600;
  line-height: 1.875rem;
  margin-top: 0.25rem;
`;
