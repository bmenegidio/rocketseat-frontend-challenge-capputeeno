import styled from 'styled-components';

export const ProductFreight = styled.span`
  color: ${(props) => props.theme.colors.textDark};
  font-size: 0.75rem;
  font-weight: 400;
  line-height: 1.2rem;
  margin-top: 1.5rem;
`;
